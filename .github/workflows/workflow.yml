on:
    push:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Build
              run: |
                  echo "Build"
<<<<<<< HEAD
            - name: Run sample bash script
              run: ../../hello.sh
=======
            - name: Build Code
              run: |
                  hello.sh
>>>>>>> a3b9a8393f49ea45c4b134d46d502f60360a5dff
            - name: Test
              run: |
                  echo "Test"
            - name: Deploy
              run: |
                  echo "Deploy"

    check:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Install Dummy Dependencies
              run: |
                  echo '{"dependencies": {"dummy-package": "1.0.0"}}' > package.json
                  npm install
            - name: Check
              run: |
                  echo "Check"
